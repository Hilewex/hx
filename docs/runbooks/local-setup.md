# Local Setup

## Services
Run `docker compose -f infra/docker/docker-compose.yml up -d` to start local services (Postgres, etc.).

## Persistence Validation
See [DB Validation Runbook](./db-validation.md) for instructions on validating the PostgreSQL foundation.
